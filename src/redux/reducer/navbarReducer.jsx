const navbarState = {
    data: {
        nama_comp: 'IMDADU COMPANY',
        logo: 'INI LOGO'
    }
}

const navbarReducer = (state = navbarState) => {
    return state;
}

export default navbarReducer